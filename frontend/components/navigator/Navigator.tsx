import { Text } from "react-native";
import React from "react";
import { StyledView } from "./StyledNavigator";

export default function Navigator() {
  return (
    <StyledView>
      <Text>Navigator</Text>
    </StyledView>
  );
}
